package aulas;

public class Aula_4_V_01 {
    /**
     * Método construtor:
     * 
     * O método construtor é um tipo especial de método em uma classe Java que é
     * chamado automaticamente quando um objeto dessa classe é criado. Ele é
     * responsável por inicializar o objeto com os valores iniciais desejados.
     * 
     * Aqui estão algumas características importantes sobre os métodos construtores:
     * - O nome do método construtor deve ser igual ao nome da classe.
     * - O método construtor não possui um tipo de retorno especificado (nem mesmo
     *   void).
     * - Um objeto pode ter vários construtores com diferentes listas de parâmetros
     *   (sobrecarga de construtores).
     * - Se nenhum construtor for explicitamente definido na classe, o Java fornecerá
     *   automaticamente um construtor padrão (sem parâmetros) chamado construtor
     *   padrão.
     * 
     * Exemplo de uso de um método construtor:
     * 
     * Pessoa pessoa = new Pessoa();
     * 
     * Neste exemplo, "Pessoa" é a classe, "pessoa" é o objeto que está sendo
     * criado, e "new Pessoa()" é a chamada ao método construtor que define
     * o objeto "pessoa".
     * 
     * Em resumo, o método construtor é executado automaticamente quando um objeto é
     * instanciado e é utilizado para inicializar o objeto com os valores padrões
     * desejados. Ele é essencial quando se deseja garantir que um objeto seja
     * criado com atributos pré-definidos.
     */


}
