package aulas;

public class Aula_4_S_01 {
/**
 * Método:
 * Em Java, um método é definido dentro de uma classe e abaixo segue a estrutura
 * básica:
 * 
 * Em Java, o nome do método deve seguir algumas convenções de nomenclatura:
 * - O nome do método deve começar com uma letra minúscula.
 * - Se o nome do método for composto por múltiplas palavras, cada palavra
 *   subsequente deve começar com uma letra maiúscula (camel case). Por exemplo:
 *   calcularMedia, obterNomeCompleto.
 * - Evite usar abreviações ou siglas obscuras no nome do método, pois isso pode
 *   dificultar a compreensão do código.
 * - É recomendado escolher nomes descritivos e significativos que indiquem
 *   claramente a função ou ação realizada pelo método.
 * - É importante evitar usar palavras reservadas da linguagem Java, como int,
 *   class, public, entre outras, como nomes de métodos. Isso pode causar erros
 *   de compilação.
 * 
 * [modificador] tipoDeRetorno nomeDoMetodo([parâmetros]) {
 *     // corpo do método
 *     // código a ser executado
 *     // retorno, se houver
 * }
 * 
 * Onde:
 * - [modificador] é um opcional que pode indicar a visibilidade e modificação do
 *   método, como public, private, protected, static, entre outros.
 * - tipoDeRetorno é o tipo de dado que o método retorna. Pode ser um tipo
 *   primitivo (como int, double, boolean, etc.) ou um objeto.
 * - nomeDoMetodo é o nome dado ao método, seguindo as convenções de
 *   nomenclatura de Java.
 * - [parâmetros] são os argumentos que o método pode receber. Eles são opcionais
 *   e podem ser de qualquer tipo de dado válido em Java.
 * 
 * O corpo do método é delimitado por chaves {} e contém as instruções que serão
 * executadas quando o método for chamado. É nesse bloco de código que você
 * escreve as ações que o método deve realizar.
 * 
 * Exemplo de um método em Java:
 * 
 * public int soma(int a, int b) {
 *     int resultado = a + b;
 *     return resultado;
 * }
 * 
 * Neste exemplo, temos um método chamado soma que recebe dois parâmetros do
 * tipo int e retorna um valor do tipo int. O corpo do método realiza a operação
 * de soma dos valores recebidos e retorna o resultado.
 * 
 * É importante lembrar que, se o método não tiver um tipo de retorno (void), não
 * é necessário usar a instrução return. Caso contrário, é obrigatório que o
 * método retorne um valor do tipo especificado pelo tipoDeRetorno.
 */

}
